# Theme list

To submit you own theme, make a backup of your app settings, paste it in this folder:
* Keep only the following fields!
    * ui
    * color
    * status_bar
    * wallpaper

* Name your theme with a simple name, underscore_case
* Add an image (not optional for visual feedback and integration in the app), named the as the json
